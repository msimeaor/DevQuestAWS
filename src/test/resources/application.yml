server:
  port: 8888
security:
  jwt:
    token:
      secret-key: s3kr3tK3YY
      expire-length: 3600000
dotenv:
  enabled: true
  systemProperties: true
spring:
  application:
    name: api
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
  devtools:
    restart:
      enabled: true
      exclude: static/**,public/**